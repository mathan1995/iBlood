import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class Main extends Component {
    render() {
        return (
            <View>
                <Text> Hello from Main component </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
